server {
  listen 80;
  server_name {{ service_host }};
  root {{webroot}};
  index index.html;
  location ~ /.well-known {
    allow all;
  }
  location / {
    return 301 https://$host$request_uri;
  }
}
